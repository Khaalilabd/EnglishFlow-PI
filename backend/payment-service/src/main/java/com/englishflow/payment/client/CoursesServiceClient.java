package com.englishflow.payment.client;

// Feign client removed — enrollment is done via RestTemplate in PaymentService
// to avoid Feign's limitation with @RequestParam on POST endpoints
public interface CoursesServiceClient {
}
